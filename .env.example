# === REQUIRED ON RENDER (Backend) ===
SECRET_KEY=           # Strong random string (run: openssl rand -hex 32)
# DB Configuration (Production/Supabase)
DB_HOST=db.ndwnwzwuomwwyfthhdwb.supabase.co
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-password
DB_NAME=postgres
# DATABASE_URL=sqlite:///./supplychain.db # Fallback if individual fields are not set
FRONTEND_URL=         # e.g. https://your-app.vercel.app

# Cookies (MUST be these values in production)
COOKIE_SECURE=True
COOKIE_SAMESITE=none
COOKIE_DOMAIN=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_API_KEY=
GOOGLE_REDIRECT_URI=  # e.g. https://your-backend.onrender.com/api/auth/google/callback

# SMTP (Email)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Internal Service Key
INTERNAL_SERVICE_KEY=
